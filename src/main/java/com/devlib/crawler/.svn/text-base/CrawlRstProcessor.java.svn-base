package com.devlib.crawler;

import com.devlib.crawler.crawlers.CrawlerResult;

public interface CrawlRstProcessor {
	void handleCrawlRst(CrawlerResult rst);

	boolean isUrlCrawled(String url);
}
